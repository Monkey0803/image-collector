# Image Collector 权限与数据边界

## 权限用途

`activeTab` 用于用户主动打开扩展后的当前页面扫描；`tabs` 用于读取当前窗口的标签页列表并支持多页面采集；`scripting` 用于向用户选择的页面注入扫描函数；`downloads` 用于提交普通图片和 ZIP 下载；`storage`、IndexedDB 和 `contextMenus` 分别用于保存设置、素材库数据和右键操作；`sidePanel` 用于侧边栏界面。

## 权限范围结论（2026-09-11 真机验收）

扩展保留 `<all_urls>`。原因是多页面采集和 iframe 扫描需要访问用户明确选择的不同网站，跨域图片在无 `Access-Control-Allow-Origin` 时仍要能进入 ZIP 下载。缩小到具体域名会直接破坏已声明的多页面与跨域能力，因此本次评估的结论是不收窄权限，继续依赖以下边界：

- 扫描范围只取当前活动标签页或用户显式勾选的标签页；`chrome://`、Chrome 商店等受保护页面由 `tabCanBeScanned` 拒绝并标记为 partial 扫描。
- 远程图片的读取失败（防盗链 403、404/410、超时、异常 `Content-Type`、超出字节预算）都会被归类保留为可重试状态，不会写入 ZIP。

## 数据边界

- 图片、缓存、素材集合和历史记录只保存在当前浏览器的本地存储中。
- 设置同步只同步扫描规则、站点适配器和下载偏好，不同步图片、缓存和历史记录。
- ZIP 读取会校验响应类型，并限制图片数量、总字节数和处理时间。
- 远程图片可能因为登录状态、防盗链、CORS 或临时 URL 失效而无法读取；此类失败应保留可重试状态。

## 发布前安全验收

- [x] 检查扩展只向用户选择的页面发起扫描操作。（真机：扫描范围取当前活动标签页或用户显式勾选的标签页；`chrome://` 页面被拒绝并标记为 partial，界面不卡 loading。）
- [x] 检查异常 `Content-Type`、重定向、超时和过大响应不会进入 ZIP。（真机：`text/html` 与 `application/octet-stream` 归类为 `invalid-content-type` 被拒；302 正常跟随；永不响应的请求归类为 `timeout`；256 MB 字节预算下超额条目归类为 `zip-limit`，ZIP 未压缩总量 240 MB 未突破上限。）
- [x] 检查导出配置不包含图片内容、缓存、集合关系以外的敏感页面数据。（真机：导出文件仅含 `version`、`exportedAt`、`settings`，不含图片地址、`data:` 内容或缓存数据。）
- [x] 检查清理素材的操作仍通过扩展内可恢复或明确的用户操作触发。（真机：清理入口经 `window.confirm` 确认，取消后素材数量保持不变。）
